# 消融工程协议（Ablation Engineering Protocol）

版本：v1.0（2026-09-10，从 NWPU/matrix300 消融矩阵的实践中固化）。项目无关，可整体复用。

## 0. 核心思想

消融实验的工程价值 = **可归因性**（臂间差恰好是设计轴）× **可复现性**（同输入逐位重建）×
**可累积性**（加新臂不破坏旧结论）。本协议的全部规则服务于这三条。总纲一句话：

> **新功能的默认状态不是"关着的开关"，而是"不存在的键"。**

## 1. 入口铁律

1. **单一参数化入口**：所有实验臂经同一个脚本进入（`<entry>.sh <arm>`），臂定义集中在
   一个 case 块内。禁止复制 runner 产生平行入口——漂移都从复制开始。
2. **公共块双保险**：
   - 所有旋钮在公共块**强制 export 默认值**（调用方 shell 的污染被覆写）；
   - 所有"只有部分臂会设置"的键在公共块 **unset**（防止前一臂/前次诊断会话残留渗透）。
   - 新增任何臂导出新键时，**同步补 unset 清单**（这是最常漏的一步，独立审计必修项）。
3. **协议开关与臂定义正交**：epoch 数、验证频率、选择策略等"协议"由独立变量
   （如 `PROTOCOL=<dev|matrix|full>`）选择，臂 case 只改模型/训练语义轴。
   协议块同样先 unset 再按 case 设置。
4. **调用约定固化**：启动横幅必须回显完整契约（全部旋键值、指纹、RUN_TAG、seed），
   任何一次运行都能从日志反推全部输入。

## 2. 配置键纪律（键缺席原则）

1. 新模块的配置键**只在启用时注入**（条件 dict / 条件展开），关闭时该键在解析后的
   配置里**不存在**——不是 `enabled=False` 的键。理由：只要实例化（哪怕禁用），
   参数注册与随机初始化流就变，旧臂复现性即破。
2. 派生键同理：仅某子模式需要的键（如特定 loss 模式的超参）只在该模式下物化。
3. **trainer 侧与模型侧旋钮分离**：损失加权、LR 调度、验证策略等训练期旋钮不进
   模型配置（不改指纹）；凡进入模型配置的键（含 `train_cfg` 采样器参数）都视为
   架构轴，改动即新指纹、即不可与旧臂直比。
4. 每个环境旋钮**全仓只有一个读取点**；默认值 = 历史行为；非法值 raise（不静默纠正）。
5. 非法组合守卫：启用新模块但缺少其依赖（如"渲染器需要 dense 模式"）直接报错。

## 3. 指纹契约（self-describing artifacts）

1. 定义：对**解析后的最终配置**做规范化后取哈希（架构 ID 为人类可读谱系标签，
   哈希载荷只含配置内容——标签/版本号变更不得扰动哈希）。
2. 三个落点：训练启动时打印进日志；存进每个产物（checkpoint/推理 manifest）；
   加载时校验（契约不匹配拒绝重建）。
3. 三大用途：
   - **回归验收**：改代码后重建全部臂，指纹与历史 run 日志逐位一致 = 旧臂零扰动的
     唯一充分证据（读代码不算数，哈希才算数）；
   - **溯源**：任何产物可反查它由哪个配置构建；
   - **跨机漂移检测**：同配置两机指纹一致 → 差异被隔离到数据顺序/硬件层。
4. trainer 侧旋钮的判别式：改了它指纹不变（a3r≡a3）；改模型键指纹必变（r3≠pb）。

## 4. 四道验证门（任何接线改动必须全过）

| 门 | 内容 | 工具形态 |
|---|---|---|
| G1 静态 | `bash -n` 全部脚本、`py_compile` 全部 py、DRY_RUN 完整协议差分 | 语法/干跑 |
| G2 构建断言 | 用**真实臂 env 重放**（dump-env 钩子导出→harness 重放）构建每臂模型；结构断言（新模块 None/非 None、可训练参数数、配置值）；全部臂**两两 diff**，差异必须恰好落在设计轴 | `verify_arms.py` |
| G3 指纹回归 | 重建指纹 vs 已跑 run 日志指纹，逐位一致 | 同上 |
| G4 污染攻击 | `env -i` + 对每个旋钮注入恶意值，重放 G2，臂环境必须与干净态逐位相同 | 对抗测试 |

发布（commit/push）前加第五道：**独立审计**（见 §8）。

## 5. 运行纪律

1. **排队**：顺序队列用单进程前台 series 一次调用（`series.sh arm1 arm2 …`），
   **不用跨启动等待器**；确需等待时只 grep **文件标记**（日志行/产物存在性），
   绝不 pgrep/pkill 进程（模式串自匹配是双发/空转事故的根源）；杀进程只按显式 PID
   且杀整树（先枚举子进程；杀包装进程前确认无孤儿脚本）。
2. **共卡禁令**：推理/探针与多卡 DDP 训练共卡，仅当显存预算有实测余量时允许，
   且按"更重的新臂可能吃掉余量"从严评估——先例不自动迁移。
3. **产物存档**：每臂按多选择依据各存一份权重（bestS/bestB/bestC/last）+ 续训锚点；
   完赛臂必须跑标准推理入口导出 per-image evidence（跨机复查与 bootstrap 的唯一输入）；
   原始日志不入库，但结果文档必须指向带 PID 的日志全名。
4. **口径**：报告主口径预先固定（推荐 last-epoch 单协议），多口径全部存档；
   best-单点含选最大偏差（选择伪影），臂间比较以平台均值为主、单点为辅。

## 6. 实验设计与结论纪律

1. **判据预登记**：采纳线、噪声带、机制验收遥测在跑之前写死，不许事后挪门柱。
2. **效应宣称线**：以同机双 seed 重复实测噪声为标尺（本项目 ~0.008）；低于线的臂间差
   只说"无显著差异"，不宣称正/负效应。
3. **不确定性**：关键臂间差配 paired bootstrap CI（per-image 重采样）；点估计不过线
   不得锚定决策。
4. **依赖 ≠ 贡献**：冻结权重干预（移除某输入）度量依赖性；从头训练对照度量净贡献；
   两者差数倍是常态（强条件信号会被重度依赖），论文里分开表述。
5. **oracle 是上限不是预测**：冻结权重 GT 替换给出的天花板，不能线性外推为训练后收益。
6. **单变量 + 阴性对照**：矩阵链每级只动一个轴；保留一条"朴素实现"行作阴性对照
   （如"画布本身不够准时 dense 倒贴"），比单调链更能证明机制。

## 7. 双机协作（要点）

1. 谁产出结论谁推送；小步快推；分叉先 fetch → rebase → push（热点文件改动前必先 fetch）。
2. 跨机直比不作数（机器偏移可实测达效应量级）；结论效力：同机同 seed > 同机异 seed > 跨机。
3. 开发与评测分离：任何机器可开发，**评测一律在基准机**；开发机数字只作方向参考。

## 8. 独立审计门

1. 实现审计（接线改动后）：互不干扰（G2/G4 视角）+ 变量单一 + 指纹回归 + 提交卫生；
   判决 PASS-WITH-FIXES 时**修复并复验**后才可提交。
2. 结论审计（关键读数后）：数字从原始产物独立重算、内部一致性、措辞越界检查
   （Dice/IoU 混淆、斜率线性化外推、上限当预测等高频错误清单）。
3. 审计者与实现者分离（子代理/他人）；审计发现一律留痕进结果文档。

## 9. 新项目落地清单（最短路径）

1. 定单一入口骨架（公共块 + 协议 case + 臂 case + 契约回显 + dump-env 钩子）；
2. 配置链实现键缺席注入（条件 dict 模式）；
3. 实现指纹契约（规范化哈希 + 三落点）；
4. 写 verify_arms harness（G2+G3），第一天就跑，之后每次接线改动必跑；
5. 首个双 seed 重复尽早跑一次，标定本机噪声带（此后所有宣称线的分母）；
6. 建证据导出入口与 paired bootstrap 工具（一次性投入，全程复用）；
7. 把 §5 的运行纪律写进运维脚本注释（等待器禁令、共卡禁令、杀树规则）。

## 附录：本仓实例映射

| 规则 | 实例 |
|---|---|
| 键缺席 | `canvas_renderer_cfg`/`decoder_tail_refiner_cfg`/P2-R1 键均条件注入 |
| 防泄漏 | dev.sh 公共块 unset 清单（审计 P1 = 漏 4 个 tail 数值键，已补） |
| 指纹回归 | 11 臂重建 vs run 日志逐位一致（2026-09-10 审计实证） |
| trainer/模型分离 | a3r(mask-ramp) 指纹=a3；RCNN_SAMPLER_NUM 在 train_cfg → 改指纹 |
| 依赖≠贡献 | box 探针 drop_box −0.0357（依赖）vs P→PB +0.014（贡献） |
| oracle 纪律 | canvas oracle 上限 +0.013~0.019 仅作 R3 天花板，不作收益预测 |
| 等待器禁令 | pgrep 自匹配双发事故（2026-09-10）后全面改文件标记 |
| 独立审计 | 探针实现审计（P0 修复）→ 结论审计（CI 补齐）→ 接线审计（P1 修复）三连 |
