# NWPU P2-v2 开发协议 100ep：A0 / A1 / A2e 训练结果与结论

日期：2026-09-07。范围：本机完成的三臂完整训练（A0/A1/A2e），各 100 epoch、
每 epoch 全量验证。设计文档见 [p2_v2_redesign_design.md](p2_v2_redesign_design.md) §3；
矩阵背景见 [nwpu_fi_matrix_plan.md](nwpu_fi_matrix_plan.md)。

状态（2026-09-09 定稿）：**A2/R1 已结案——无可靠增益**。时间线：机器2 同机配对
A2−A0=+0.023（seed44，§3b.1）→ lthpc 同机复跑 A2 完赛 −0.005（§3b.6）→ 机器2
seed45 复判 +0.003（§3b.6，落入 A0 自身 seed 波动 ±0.008 内）→ lthpc 第一门探针
裁决推理期通道 +0.0002 IoU（`p2_first_gate_a2_results.md`）。R1 降级为
seed 敏感/不可复现方向；当前正效应候选只剩 A3/UDPR（lthpc dev100 best +0.028 /
last +0.013），机器2 已开 300ep matrix（p/a0/a3 在跑，§3b.7）。

## 1. 公共协议（三臂一致）

- 数据：NWPU VHR-10，520 train / 130 val，全量、无子集（选择集=报告集，val==test 按用户裁决）。
- 训练：100ep 压缩余弦（total_optimizer_steps=6500），warmup 100 步，lr 5e-4，wd 0.05，
  4 GPU × batch1 × accum2（有效 batch 8），AMP，EMA 跟踪但选优用 raw，seed 44。
- 空间契约（fi）：`FINAL_MASK_COORDINATE_MODE=full_image` +
  `FINAL_MASK_LOSS_MODE=roi_balanced_dice`（绑定，非可调超参）。
  三臂运行签名一致：`debug_mask_target_fill=0.0177~0.0179`（legacy roi_local 为 0.5623），
  roi_balanced 三段遥测齐全，推理 `mask_postprocess=full_image_resize`。
- 增强：hflip 0.5 + vflip 0.5 + 多尺度抖动 0.5@value（896–1152 五档，先抖动再定回 1024）；
  rot90 本轮裁决不开（设计文档 §0 audit P2-1），三臂统一。
- 入口：`scripts/ablations/vhr10_p2v2_dev.sh <arm>`（单一参数化入口，防漂移；
  六臂构建已由 `scripts/smoke/verify_p2v2_arms.py` 全部断言通过）。

## 2. 三臂定义（相对 A0 的唯一差异轴）

| 臂 | P2 | loss 模式 | β | Δ_max（cap） | 其余 |
|---|---|---|---|---|---|
| A0 | off | — | — | — | PBM（points_box_dense），D5-B 配方：α0.5 可学习 global_sigmoid 门、PE mask_downscaling 解冻（4684 参数，lr_mult 0.1） |
| A1 | on | boundary（原版 P2） | 0.10 | 0.50（±0.05） | = A0 |
| A2e | on | correction_keep（R1） | 0.30 | 1.00（±0.30，sat_thr 0.297） | margin 1.0，λ=0.1，w_aux 0.05 |

架构指纹（日志实测）：A0 `9489ea70…`，A1 `efa0a623…`，A2e `775f2a00…`
（A2 默认臂 `0a4335bc…`，另一台机器）。臂间差异已验证恰好落在设计轴上
（p→pb: mode；pb→a0: mode+md；a0→a1: enabled；a1→a2: loss/margin/keep；a2→a2e: β+Δ）。

## 3. 主结果（segm/mAP，每 epoch 全量 130 图验证）

| 臂 | best（epoch） | E100 | 平台期 E91–100（均值±std） | best−A0 | 平台−A0 | 训练时长（日志跨度） |
|---|---|---|---|---|---|---|
| **A0** | **0.6669**（E36） | 0.6464 | **0.6467**±0.0011 | — | — | 3h55m |
| A1 | 0.6530（E32） | 0.6425 | 0.6433±0.0005 | **−0.0139** | −0.0034 | 4h22m（OOM 前次尝试已隔离，不计） |
| A2e | 0.6590（E32） | 0.6367 | 0.6369±0.0014 | **−0.0079** | −0.0098 | 4h27m |

Top-5（选择集口径，供噪声带参考）：
- A0：E36 0.6669 / E41 0.6587 / E74 0.6559 / E45 0.6549 / E67 0.6543
- A1：E32 0.6530 / E66 0.6528 / E67 0.6496 / E74 0.6492 / E52 0.6485
- A2e：E32 0.6590 / E67 0.6546 / E33 0.6540 / E52 0.6531 / E74 0.6528

A0 锚点对照：best 0.6669 已超 legacy 400ep Full 0.6476 与 ft200 0.6617（约 1/4~1/6 预算）；
三臂 best 都出现在 E32–E41 早段，平台期差距（A1 −0.003 / A2e −0.010）比 best 差距更能说明问题，
因为 130 图选择集上 best 单点含选择噪声（A0 自身 E36 与 E41 差 0.008）。

## 3b. 机器2 同机四臂网格与 A2 回填（2026-09-08）

机器2 = machine2/fast-whu150 工作机（单卡 RTX 4090 48GB / 臂，76 核）。协议与本表 §1
逐项一致，仅并行结构不同：**1 GPU × batch1 × accum8（有效 batch 8）、6500 优化步、
seed 44、AMP**。同 seed + world_size=1 ⇒ 四臂（p/pb/A0/A2）**样本顺序完全相同，
臂间为配对比较**（lthpc 侧三臂同理自配对）。代码 00c9dab（A2）；
boundary 路径相对 624165c 逐字节未变（唯一删除行是原公式原样移入
`if loss_mode == "boundary"` 分支，R1 为纯增量），跨版本可比性成立。
A2 架构指纹 `0a4335bc…`。

### 3b.1 主结果（segm/mAP，与 §3 同口径）

| 臂 | best（epoch） | E100 | 平台期 E91–100（均值±std） | best−A0 | 平台−A0 | 训练时长 |
|---|---|---|---|---|---|---|
| p（纯点） | 0.6541（E79） | 0.6394 | 0.6394±0.0021 | −0.0049 | +0.0041 | 8h56m |
| pb（点+框） | 0.6532（E64） | 0.6343 | 0.6340±0.0004 | −0.0058 | −0.0013 | 8h43m |
| **A0（PBM）** | **0.6590**（E63） | 0.6366 | 0.6353±0.0013 | — | — | 9h17m |
| **A2（PBM+R1）** | **0.6718**（E58） | **0.6577** | **0.6578**±0.0021 | **+0.0128** | **+0.0225** | 9h18m |

Top-5：p：E79 .6541 / E76 .6516 / E73 .6512 / E75 .6481 / E58 .6480；
pb：E64 .6532 / E60 .6475 / E74 .6448 / E70 .6431 / E55 .6400；
A0：E63 .6590 / E43 .6572 / E50 .6546 / E70 .6523 / E79 .6518；
A2：E58 .6718 / E70 .6693 / E76 .6693 / E63 .6687 / E79 .6681。

**A2 与其余三臂分布完全分离**：A2 收敛段（E81–100）最低 0.6540 高于 p/pb/A0
任何一轮的最高值（0.6443）；A2 的第 5 好（0.6681）已超其余三臂 best（0.6590）。

### 3b.2 Prompt 阶梯读数（平台期 E91–100；E81–100 窗口读数一致 ±0.004）

- p → pb：**−0.005**（box 提示在 NWPU 为负贡献）；
- pb → A0：+0.001（dense+PE 填回 box 损失）；
- **净 p → A0：≈0（−0.004~+0.000 双窗口）——三件套 prompt 相对纯点无增益**。
  与 WHU fi 契约 +0.024~0.037 形成对照，印证 nwpu_fi_matrix_plan D9
  （提示几何按 WHU 建筑足迹调参，不适配 NWPU 尺度谱）。
- **A0 → A2：+0.023——四臂唯一实质增益，全部来自 R1（correction_keep）**。

### 3b.3 机器效应标定（跨机格的解读前提）

双侧 A0（同臂同协议同 seed）：lthpc 0.6467±0.0011 vs 机器2 0.6353±0.0013
（E91–100），**机器偏移 +0.011**（best 口径 +0.008：0.6669 vs 0.6590）。因此：

- §3 表中 A2 若直接跨机对 A0@lthpc 为 +0.011，**几乎恰被机器偏移抵消，不构成结论**；
- 同机配对口径 A2−A0 = **+0.023**（§3b.1），机器偏移校正后 A2 的 lthpc 等效平台
  ≈ 0.669（供参考）；
- 定稿依据排序：机器2 四臂配对（本节）> lthpc 队列同机 A2（待出）> 跨机直比（不作数）。

### 3b.4 A2 机制遥测摘记（默认包络 R1，对照 §5 的 A2e）

- 饱和阈值 0.0495（=0.99×β0.10×Δ_max0.50），修正全程贴上限运行：
  活跃 ROI 平均 |delta| ≈ 0.04–0.05，饱和计数从 E1 的 0 升至中后段 ~5–6/30–64 ROI；
- `p2_boundary` 损失占比从 E1 的 2.1% 升至 E100 的 25.4%（其他损失衰减所致，
  与 A2e 同构）；R1 两部分中 corrective 主导；
- 与 A2e（3×包络、末段饱和率 23–27%、平台 −0.010）对照：
  **R1 的增益只在默认保守包络下出现，包络放大 3 倍即翻负**——
  包络约束本身是 R1 有效性的一部分，不是待解除的瓶颈。

### 3b.5 日志与工件（机器2，便于复查）

| 项 | 路径（相对 `portable_sam2_explicit_coarse/`） |
|---|---|
| A2 训练日志 | `logs/ablations/vhr10_p2v2_dev100_a2_r1_tr1.0_va1.0_20260907_144606_pid4115797.log` |
| A0 训练日志 | `logs/ablations/vhr10_p2v2_dev100_a0_pbm_d5b_tr1.0_va1.0_20260908_003110_pid151751.log` |
| p 训练日志 | `logs/ablations/vhr10_p2v2_dev100_p_point_tr1.0_va1.0_20260908_002920_pid149405.log` |
| pb 训练日志 | `logs/ablations/vhr10_p2v2_dev100_pb_tr1.0_va1.0_20260908_002920_pid149406.log` |
| A2 ckpt | `/data1/wangcheng/checkpoint/portable_sam2_explicit_coarse/ablations/vhr10_p2v2_dev100_a2_r1_tr1.0_va1.0/`（best_model_epoch58.pth, last_model_epoch100.pth） |
| A0/p/pb ckpt | 同根目录 `…_{a0_pbm_d5b,p_point,pb}_tr1.0_va1.0/`（best=E63/E79/E64, last=E100） |
| 启动方式 | `CUDA_VISIBLE_DEVICES=<g> NPROC_PER_NODE=1 GRAD_ACCUM_STEPS=8 bash scripts/ablations/vhr10_p2v2_dev.sh <arm>`（单卡参数化为本机两行 `:-` 改动，未入库） |

### 3b.6 A2 结案：lthpc 同机复跑 + 机器2 seed45 复判（2026-09-09）

| 配对样本 | A0 平台 E91–100 | A2 平台 E91–100 | A2−A0 |
|---|---|---|---|
| 机器2，seed44 | 0.6353±0.0013 | 0.6578±0.0021 | **+0.0225** |
| 机器2，seed45 | 0.6436±0.0017 | ≈0.6466（E100=0.6476，best 0.6585@E31） | **≈+0.003** |
| lthpc，seed44 | 0.6467±0.0011 | 0.6420±0.0017（best 0.6513@E63） | **−0.0047** |

- 三次独立配对抽样给出 +0.023 / +0.003 / −0.005：**均值 ~+0.007，方差远大于均值，
  R1 无可靠正效应**。seed44 的 +0.023 判定为幸运抽样。
- lthpc A2（`vhr10_p2v2_dev100_a2_r1_tr1.0_va1.0_20260908_001854_pid3679960.log`）
  与机器2 A2 同代码 `00c9dab`、同指纹 `0a4335bc`、同协议——排除版本漂移；
  P2 遥测两机几乎重合（损失占比 1.9%→22.9% vs 2.1%→25.4%），机制照常运转、
  收益不兑现。**训练期 debug 遥测不是 R1 成败的预测器**。
- 机器2 A0 双 seed 平台差 0.0083 = 同臂异 seed 噪声标尺；dev100 层任何低于
  该量级的臂间差（含 p→pb→A0 阶梯的 −0.005/+0.001）均不可分辨。
- seed45 日志：`vhr10_p2v2_dev100_{a0_pbm_d5b,a2_r1}_seed45_tr1.0_va1.0_20260908_*.log`
  （启动带 `SEED=<n>` 参数化，RUN_TAG 自动加 `_seed<n>` 后缀）。

### 3b.7 机器2 matrix300 在跑（2026-09-09 挂载，300ep 论文层）

`vhr10_p2v2_matrix300_{p_point,a0_pbm_d5b,a3_pbm_udprk64}_tr1.0_va1.0_20260909_*.log`：
单卡×累积8（有效 batch 8）、seed44、三口径选优（segm/bbox/composite）、val 每 5ep。
p 在 GPU2，a0/a3 共享 GPU3。A3 承担 UDPR 跨机复现 + 300ep 长程裁决双任务；
结果出来后另行回填。

## 4. A0 完整 COCO 指标（新契约推理，best 与 last）

| 口径 | segm/mAP | mAP_50 | mAP_75 | mAP_m | mAP_l | AR@100 | bbox/mAP |
|---|---|---|---|---|---|---|---|
| best（E36） | 0.6669 | 0.9487 | 0.7133 | 0.5429 | 0.7240 | 0.7078 | 0.6764 |
| last（E100） | 0.6464 | 0.9170 | 0.6907 | 0.4748 | 0.7144 | 0.6832 | **0.7089** |

bbox 与 segm 双时间尺度：segm 平台在 E60–80（E91–100 均值 0.6467），
bbox 到 E100 仍在涨（E100 0.7089 > best 轮 0.6764）。100ep 下 bbox 欠收敛，
这直接影响"最终矩阵用多长 schedule"的决策（nwpu_fi_matrix_plan D1 两层协议待定稿）。

## 5. A2e 机制遥测（R1 + 扩大包络）

训练全程 `[DEBUG-P2BR]` 分 epoch 汇总（饱和阈值 0.297 = 0.99×β×Δ_max）：

| epoch | 支持内饱和率 | 有效 ROI | 备注 |
|---|---|---|---|
| 1 | 0% | 61 | 冷启动 |
| 10 | 28.3% | 151 | |
| 20 | **49.2%**（峰值） | 154 | |
| 32 | 38.0% | 174 | best 轮 |
| 50 | 38.5% | 99 | |
| 80 | 29.5% | 171 | |
| 100 | 26.7% | 184 | 收敛态 |

- 饱和率走势是"中程冲高后回落"：E20 峰值 49%，末段稳定在 23–27%，
  **修正幅度没有钉死在新 cap 上**——扩大 3 倍的包络（±0.05→±0.30）不是后期的绑定约束。
- P2 损失绝对量全程近似恒定（≈0.055），占总损失比例从 5.7%（E5）升到 22.8%（E100），
  原因是其他损失衰减而 P2 不衰减；R1 两部分中 corrective 主导（≈0.054），
  keep 部分始终 ≈0.001（margin=1.0 下 keep 区几乎不产生违规）。
- 中程（E41）观察记录：当时饱和率 43%，且 refinement 增益为负（dice −0.0021）——
  更大的修正没有兑换成更好的 mask。

## 6. 结论

1. **A1：原版 P2（boundary 监督）在新消费端下无增益**——best −0.0139、平台 −0.0034，
   与 WHU D 系列方向一致，NWPU 全量 100ep 复现。
2. **A2e：R1 监督 + 3 倍包络仍无增益**——best −0.0079、平台 −0.0098。
   叠加遥测（饱和率不钉死、P2 损失不衰减但兑换不成 mAP、refinement 增益为负），
   **"包络/参数是瓶颈"的假说被否定：给更大的修正预算，模型学会了用，
   但用出来的修正不改善最终 mask**。
3. **（2026-09-09 结案）A2（默认包络 R1）无可靠增益，seed44 的 +0.023 为幸运抽样**。
   三次配对抽样：机器2 seed44 +0.023 / 机器2 seed45 +0.003 / lthpc seed44 −0.005
   （§3b.6）；叠加第一门探针（推理期通道 +0.0002 IoU，CI 判死）、双 seed 遥测
   与成败无判别力——**R1 作为 logit 残差修正的整条路线（boundary/R1 × 包络大小）
   2×2 四格全部无效或不可复现**。与 2026-09-08 版"收回判死"的中间结论相比，
   本版为终审：seed45 复判数据当时未出。
4. **A0 是当前最优配方**（PBM + D5-B：α0.5 可学习门 + PE mask_downscaling 解冻 + P2 off），
   100ep 即超 legacy 长训锚点。后续候选方向：R3（从 P2 特征直接渲染 dense 画布，
   走 canvas 通道而非 logit 残差）——已有 WHU 冻结诊断 GT-canvas@α1.0 +0.0154 的通道价值证据，
   NWPU 侧前置测试（canvas 准确率探针，blend r=0/0.5/1.0）已排队待 GPU 空闲执行。
5. **（2026-09-08 回填）P/PB 基线臂已在机器2 完赛**（§3b.2）：box 增益 = PB−P = −0.005、
   dense 包增益 = A0−PB = +0.001，**净 prompt 阶梯 ≈0**——NWPU 上三件套 prompt
   相对纯点无增益，矩阵叙事重心应移至 R1；FI 矩阵的行间价值主要由 WHU 侧承载。
6. 若升 400ep 论文层：优先 A2 与 A0 两臂（p/pb 已证无增量，不值得长程预算）；
   bbox/segm 错峰问题（§4）建议以 bbox-best 保留或两段式 schedule 处理。

## 7. 工件索引

| 项 | 路径 |
|---|---|
| A0 ckpt | `ablations/vhr10_p2v2_dev100_a0_pbm_d5b_tr1.0_va1.0/`（best_model.pth=E36, last_model_epoch100.pth） |
| A1 ckpt | `ablations/vhr10_p2v2_dev100_a1_p2v1_tr1.0_va1.0/`（best=E32, last=E100） |
| A2e ckpt | `ablations/vhr10_p2v2_dev100_a2e_r1_esc030_tr1.0_va1.0/`（best=E32, last=E100） |
| 训练日志 | `logs/ablations/vhr10_p2v2_dev100_{a0_pbm_d5b,a1_p2v1,a2e_r1_esc030}_tr1.0_va1.0_*.log` |
| A0 推理 | 上述 A0 目录 `inference_{best,last}_vhr10/run_manifest.json` |
| 隔离工件 | `*_a1_p2v1_crashed_oom_partial`（A1 OOM 前次尝试）、`*_a2_r1_*_stopped_for_a2e_partial`（本地 A2 让位 A2e） |
| 臂构建断言 | `scripts/smoke/verify_p2v2_arms.py`（六臂全过，指纹与真实 run 逐位一致） |
| 机器2 全套工件 | 见 §3b.5（A2/A0/p/pb 日志带 PID 全名 + ckpt 目录） |

## 8. 判读注意（caveats）

- best 单点含 130 图选择噪声；臂间比较以平台期（E91–100 均值）为主、best 为辅，两口径本文同报。
- A2e 的"饱和率"是支持区内逐像素 |delta|≥0.297 的池化比例（分子/分母分别求和后再除），
  不是 ROI 均值的均值。
- A1/A2e 尚无 best/last 完整 COCO 推理；本文 A1/A2e 只有训练期验证指标，
  与 A0 第 4 节的推理口径（同一契约、同 130 图）可比性待推理补齐后确认。
- 本系列保存 `last_model_epoch100.pth`（SAVE_LAST_MODEL=1），不存在 D 系列 E15 未存档问题。
- 单次训练、单 seed（44）；小差距的严格置信需训练重复，本文结论以"多臂同向 +
  机制遥测互证"支撑，不依赖单臂单点差。
- **跨机不可直比**：lthpc 与机器2 存在 +0.011 机器偏移（双侧 A0 标定，§3b.3），
  §3 与 §3b 两表各自内部比较；机器2 四臂因同 seed+world_size=1 样本顺序一致，
  为配对比较，证据等级高于跨机格。
- 机器2 的单卡 × accum8 与 lthpc 的 4 卡 × accum2 梯度数学等价（无 SyncBN，
  每次前向单样本归一化），残余差异为数据分片顺序与 BN running stats 积累广度，
  已并入上述机器偏移，不再单独归因。
- 本文 A2 数字（§3b）来自 2026-09-07/08 机器2 运行（git 00c9dab, dirty=1，
  含未入库的单卡 `:-` 参数化两行）；复查时以 §3b.5 日志 PID 为准。
