# K 值选择预注册（论文辩护基础）

日期：2026-09-15（K=32 在跑、K∈{16,128,256} 未跑时写定）。本文在选择数据齐备前
固定选择规则与辩护框架；数据到齐后按规则落选，不得事后改规则。
**v1.1 修订见 §6（2026-09-15 17:xx 抢救性修订，赶在当晚首批数据落地前）。**

## 1. 候选与证据层

候选 K ∈ {16, 32, 64, 128, 256}。三层证据（口径互不可混）：

| 层 | 内容 | 状态 |
|---|---|---|
| L1 冻结策略 | a3sgtm E32 平台上 prefix 截断的写入价值曲线 | 完成：饱和 @64–128，K64=渐近 94% |
| L2 热启动训练 | 远端 a3m 三基座 × 五 K（ksweep40） | 完成：饱和 @128，K64 与峰差 ≤0.003（噪声带内） |
| L3 从头训练（主判读） | 本机 A3 matrix300 × 五 K，诚实口径 | K64 锚点完成；K32 在跑；16/128/256 待排 |

## 2. 选择规则（预注册）

主口径：**平台均值（E271–300）segm/mAP**；副口径 last；伴随指标 bbox/mAP
（K 不得以牺牲 bbox 换 segm）。

1. 计算各 K 的主口径值与 K 间配对 bootstrap CI（records 已由 wrapper 自动导出）；
2. 取平台口径的 argmax 为 K\*；
3. **入选 K = 满足以下全部条件的最小 K**：
   a. 与 K\* 的差在噪声带内（配对 CI 跨零，或 |Δ| ≤ 0.008 训练噪声尺度）；
   b. bbox 相对 K\* 无显著损失（CI 下界 > −0.005）；
   c. 该 K 在 L1/L2 层同样位于其饱和带内（跨层一致性）。
4. 平局（多 K 同时满足）取更小 K（简约性：更少选中点、更省算力）。

**预期落点**（写定时的先验，非承诺）：K\* ∈ {64, 128}，入选 K = 64——由 L1/L2 的
饱和位置与"最小合格 K"规则联合推出。若 L3 显示 128 显著超 64（CI 不跨零），
则入选 K = 128，此时"梯度通道覆盖饥饿"成立并作为机制发现写入论文。

## 3. 论文辩护框架（最终指标 + 平衡性）

**最终指标维度**：五 K 的主口径曲线 + 配对 CI 图；argmax 与入选 K 标注。

**平衡性维度**（三条，全部可量化）：
1. **精度–预算平衡（膝点论证）**：入选 K 位于饱和膝部——拿到的价值 ≥ 渐近的
   94%（L1 实测），相对 argmax 的损失在噪声带内，而选中点数/计算更省；
2. **segm–bbox 平衡**：K 扫描全程 bbox 无显著退化（L2 冻结基座 bbox 逐位继承；
   L3 逐 K 检验），K 的选择不引入检测代价；
3. **稳健平衡（非调参论证）**：选择落在曲线平坦带内——三层证据（冻结/热启动/
   从头）在同一区域饱和，K 的具体取值在 [入选 K, 128] 内不敏感，故 K 不是
   精调出来的超参。

## 4. 排程与门槛

- K32 完赛（~22:00）→ 自动 best/last 推理；随后 K128 → K16 → K256 依次
  （K128 为决策关键行：定 argmax 在 64 还是 128）；
- 每档一个 EXP-ID、全量先 commit；GPU 与另一会话的 dev100 K 筛选协调；
- 全部出数后按 §2 落选、按 §3 成文，选择理由与数据一起归档本文档。

## 5. 已知边界

- L3 各 K 均单 seed；K 间噪声带用 0.008（A0/A3 双 seed 标定）作先验尺度，
  不对单 seed 差值做过解读；
- L2 为远端机器（+0.011 系统偏移），只取其 K 间序，不混绝对值；
- 中期尖峰（bestS 类）不进判读，预注册口径为平台/last。

## 6. 修订 v1.1（2026-09-15 17:xx，deadline 抢救性修订）

**修订理由**（用户裁决，2026-09-15）：投稿日期前无法完成原 §1-L3 的全部五档
matrix300 从头扫描（每档 ~9.5h × 4 档剩余）。修订发生在 dev100 筛选（E37/100，
未完赛）与 L3-K32（未完赛）数据落地之前——选择数据仍未齐备，不构成事后改规则。

**修订后的证据层**（原表保留于 §1）：

| 层 | 内容 | 状态 |
|---|---|---|
| L1 冻结策略 | 同原（饱和 @64–128，K64=渐近 94%） | 完成 |
| L2 热启动训练 | 同原（ksweep40，饱和 @128，K64 噪声带内） | 完成 |
| **L2.5 主制式短程筛选（新增，修订后的主判读层）** | machine2 dev100 a3 × K∈{32,64,128}，同 seed 同样本序配对，单卡并行 | E37/100，~23:30 完赛 |
| L3 从头全预算（降级为支撑层） | K64 锚点完成；K32 在跑（~22:00）；K128 排程下一档（决策关键）；16/256 退出 deadline 路径 | 部分 |

**修订后的选择规则（替代 §2 的主判读定义；§2 的 3/4 条与 bbox 伴随约束继承）**：

1. 主口径：**L2.5 的 E81–100 平台均值 segm/mAP**（与原平台哲学同构；dev100
   的 best/中期尖峰不进判读）；副口径 last ckpt；伴随指标 bbox/mAP
   （last ckpt 口径，K 不得以牺牲 bbox 换 segm）；
2. argmax 为 K\*；**入选 K = 与 K\* 差在噪声带内（|Δ|≤0.008）且 bbox 无显著
   损失的最小 K**；平局取更小 K；
3. 跨层一致性（原 §2.3c 继承）：入选 K 须落在 L1/L2 的饱和带内；
4. **主表口径不变**：论文主表与消融沿用 matrix300 K=64 行（时序真实：矩阵
   在筛选前即固定 K=64）。L2.5/L3 的角色是**为该选择提供事前规则化的辩护**，
   而非事后更换主表 K——除非入选 K ≠ 64 且投稿时间线允许重训（当前判定：
   不允许，a3r 等派生臂建立在 K=64 上）。

**论文表述**（诚实口径）：K 经三层证据 + 100ep 筛选（同 seed 同样本序）选定；
全部证据层显示 K=64 位于饱和带内、与 argmax 差在 run 噪声内；主表以完整
300ep 协议 K=64 报告。

**排程**（对齐 §4）：今晚 22:00 L3-K32 完赛、~23:30 L2.5 三臂完赛 → 按 §6.1-3
落选 → 若入选 K≠64，按 §6.4 处理并全文披露。

## 7. 落选记录（2026-09-15 21:4x，按 §6 规则执行）

L2.5 三臂完赛（E100/100，同 seed 同样本序，21:35 前后陆续落盘）；best/last
双 ckpt eval 于 21:4x 导出（GPU0，零失败）。

**主口径（E81–100 平台均值 segm/mAP）**：K32 0.6508±0.0024 / K64 **0.6660±0.0028**
/ K128 0.6605±0.0025 → **argmax K\* = 64**（E95-99 逐 epoch 领先 K128 无一例外）。

**规则执行**：K32 距 K\* −0.0152（出带）；K128 −0.0055（带内但非最小）；
bbox 伴随（平台）K64 0.7022 vs K128 0.7026（无损失）；跨层一致性：64 ∈ L1
（渐近 94%）/ L2（噪声带内）饱和带 → **入选 K = 64**。主表 K=64 零改动，
辩护闭环。

**论文表口径（last ckpt E100 完整 COCO）**：

| K | mask AP | AP50 | AP75 | APm | APl | AR100 | bbox AP | bbox AP75 |
|---|---|---|---|---|---|---|---|---|
| 32 | 0.6494 | 0.9291 | 0.7001 | 0.4691 | 0.7406 | 0.6840 | 0.6938 | 0.7903 |
| **64** | **0.6665** | **0.9415** | 0.7197 | **0.5051** | **0.7409** | **0.7036** | **0.7035** | **0.8165** |
| 128 | 0.6597 | 0.9371 | **0.7222** | 0.4863 | 0.7386 | 0.6980 | 0.7013 | 0.8132 |

（K64 八列取七；K128 仅 AP75 微高 +0.0025，带内。）best 口径行留内部备询：
K32 0.6766 / K64 0.6903 / K128 0.6682（segm/mAP）。

**机制遥测注记**（E64-84 均值）：修正方向一致率非单调、峰值在 K=64
（0.731/**0.759**/0.736），K=128 覆盖率最高（0.816）但写入最重（delta_abs
0.260 vs 0.165）——联合训练制式下 K 的约束从覆盖率切换为修正质量，与冻结
基座层（L2，方向一致率单调、覆盖率主导）形成制式对照。prefix 截断探针
（在训好权重上）列为后续机制分析项，不进选择规则。
